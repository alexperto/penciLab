.navbar.navbar-secondary
  .navbar-inner
    .container
      %ul.nav
        %li{ :class => ( 'active' if current_page? new_user_password_path ) }=    link_to "Forgot your password?", new_password_path(resource_name)
        /%li{ :class => ( 'active' if current_page? new_user_confirmation_path )}= link_to "Didn't receive confirmation instructions?", new_confirmation_path(resource_name)
        /%li{ :class => ( 'active' if current_page? new_user_unlock_path) }=       link_to "Didn't receive unlock instructions?", new_unlock_path(resource_name)
